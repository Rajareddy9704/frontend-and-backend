services:
  frontend:
   build: ./frontend
   ports:
    - "80:3000"
   container_name: frontend
   depends_on: 
        - backend
        
  backend:
   build: ./backend
   ports:
    - "81:3001"
   container_name: backend
